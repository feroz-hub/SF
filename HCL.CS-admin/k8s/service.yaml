apiVersion: v1
kind: Service
metadata:
  name: hcl-cs-admin
  labels:
    app: hcl-cs-admin
spec:
  selector:
    app: hcl-cs-admin
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
